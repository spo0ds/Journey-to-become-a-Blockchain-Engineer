"use strict";
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.makeVerify = void 0;
var api_utils_1 = require("@moralisweb3/api-utils");
var core_1 = require("@moralisweb3/core");
var MoralisAuth_1 = require("../MoralisAuth");
var resolvers_1 = require("../resolvers");
var requestMessage_1 = require("./requestMessage");
var makeEvmVerify = function (core, _a) {
    var network = _a.network, options = __rest(_a, ["network"]);
    return api_utils_1.EndpointResolver.create(core, MoralisAuth_1.BASE_URL, resolvers_1.completeChallengeEvm).fetch({
        message: options.message,
        signature: options.signature,
    });
};
var makeSolVerify = function (core, _a) {
    var network = _a.network, options = __rest(_a, ["network"]);
    return api_utils_1.EndpointResolver.create(core, MoralisAuth_1.BASE_URL, resolvers_1.completeChallengeSol).fetch({
        message: options.message,
        signature: options.signature,
    });
};
var makeVerify = function (core) { return function (options) {
    switch (options.network) {
        case requestMessage_1.AuthNetwork.EVM:
            return makeEvmVerify(core, options);
        case requestMessage_1.AuthNetwork.SOLANA:
            return makeSolVerify(core, options);
        default:
            throw new core_1.MoralisAuthError({
                code: core_1.AuthErrorCode.INCORRECT_NETWORK,
                message: "Incorrect network provided. Got \"".concat(options.network, "\", Valid values are: ").concat(Object.values(requestMessage_1.AuthNetwork)
                    .map(function (value) { return "\"".concat(value, "\""); })
                    .join(', ')),
            });
    }
}; };
exports.makeVerify = makeVerify;
//# sourceMappingURL=verify.js.map