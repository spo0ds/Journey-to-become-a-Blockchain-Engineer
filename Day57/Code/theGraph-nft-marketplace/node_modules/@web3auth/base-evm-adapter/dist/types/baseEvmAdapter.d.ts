import { BaseAdapter, UserAuthInfo } from "@web3auth/base";
export declare abstract class BaseEvmAdapter<T> extends BaseAdapter<T> {
    authenticateUser(): Promise<UserAuthInfo>;
    disconnect(): Promise<void>;
}
