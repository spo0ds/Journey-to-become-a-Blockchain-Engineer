/**
* ps-grab module
* @author Abdennour TOUMI <http://abdennoor.com>
*/
((ps,m)=>{
    m.exports=function(flag){
      var index = ps.argv.indexOf(flag);
       return (index === -1) ? null : ps.argv[index+1];
    };
})(process,module)
