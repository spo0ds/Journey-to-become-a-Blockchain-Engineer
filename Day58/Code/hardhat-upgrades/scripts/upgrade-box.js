const { ethers } = require("hardhat")

async function main() {
    const boxProxyAdmin = await ethers. 
 }

main()
    .then(() => process.exit(0))
    .catch((error) => {
        console.error(error)
        process.exit(1)
    })